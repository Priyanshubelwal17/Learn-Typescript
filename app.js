function printName(id) {
    if (typeof id === "string") {
        console.log(id.toUpperCase());
    }
    else {
        console.log(id);
    }
}
printName(32);
export {};
//# sourceMappingURL=app.js.map